"use client"

// The first-time free design offer is only surfaced through the PromoStrip.
// This component intentionally renders nothing so package pages stay clean.
export function PackageBonusBanner() {
  return null
}
